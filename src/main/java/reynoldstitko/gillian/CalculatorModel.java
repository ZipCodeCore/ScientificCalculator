package reynoldstitko.gillian;

/**
 * Created by gillianreynolds-titko on 1/14/17.
 */
public class CalculatorModel {

    private Double result;

    private void performBinaryOperation(Double firstInputValue, String operandType, Double secondInputValue){
        //perform +, /, *, - x^x (input 1 + 1, 2-1, 1/x, x ^ x, x ^ y)
    }

    private void performUnaryOperation(Double inputValue, String operandType){
        //perform x^2, sqrt, +/- (input 3^3; 2 sqrt)
    }

    private void performTrigFunctions(Double inputValue, String operandType){
        //Cos, Sin (input 4 cos; 2 sin)
    }

    private void performOtherFunctions(Double firstInputValue, Double secondInputValue){
        //x^y
    }
}
