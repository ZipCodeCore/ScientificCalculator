package io.michaelcarroll;

/**
 * Options for switching or rotating through different
 * Display Modes on the Calculator
 */

public enum DisplayMode {
    BINARY, OCTAL, DECIMAL, HEXDECIMAL, ROTATE
}

